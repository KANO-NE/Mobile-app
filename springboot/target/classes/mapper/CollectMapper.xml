<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.CollectMapper">

    <sql id="Base_Column_List">
        id,topic_id,user_id,time
    </sql>

    <select id="selectAll" resultType="com.example.entity.Collect">
        select
        collect.*,user.name as userName,topic.title as topicTitle
        from collect
        left join user on collect.user_id = user.id
        left join topic on collect.topic_id = topic.id
        <where>
            <if test="id != null"> and id = #{id}</if>
            <if test="topicId != null"> and topic_id = #{topicId}</if>
            <if test="userId != null"> and user_id = #{userId}</if>
            <if test="time != null"> and time like concat('%', #{time}, '%')</if>
        </where>
        order by id desc
    </select>

    <select id="selectById" resultType="com.example.entity.Collect">
        select
        <include refid="Base_Column_List" />
        from collect
        where id = #{id}
    </select>

    <select id="selectByUserId" resultType="com.example.entity.Collect">
        select collect.*,topic.title as topicTitle
        from collect
        left join topic on collect.topic_id = topic.id
        where user_id = #{id}
    </select>

    <delete id="deleteById">
        delete from collect
        where  id = #{id}
    </delete>

    <insert id="insert" parameterType="com.example.entity.Collect" useGeneratedKeys="true" keyProperty="id">
        insert into collect
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="topicId != null">topic_id,</if>
            <if test="userId != null">user_id,</if>
            <if test="time != null">time,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id},</if>
            <if test="topicId != null">#{topicId},</if>
            <if test="userId != null">#{userId},</if>
            <if test="time != null">#{time},</if>
        </trim>
    </insert>

    <update id="updateById" parameterType="com.example.entity.Collect">
        update collect
        <set>
            <if test="topicId != null">
                topic_id = #{topicId},
            </if>
            <if test="userId != null">
                user_id = #{userId},
            </if>
            <if test="time != null">
                time = #{time},
            </if>
        </set>
        where id = #{id}
    </update>

</mapper>